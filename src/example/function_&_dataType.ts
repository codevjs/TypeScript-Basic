/**
 * Type basic
 * 2. fugis && data type
 */

/**
 * string function
 * @param {string} name
 * @return {string}
 */
function getName(name : string) : string {

    return "Hello " + name;
}

console.log(getName());
